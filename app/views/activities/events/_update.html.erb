<% cache activity do %>
  <% user = activity.user %>
  <% event = activity.trackable %>
  <% event_start = event.starts_at.in_time_zone(@subscriber.time_zone).strftime('%b %d %Y') %>
  <li>
  <span class="activity_date"><%= activity.created_at.in_time_zone(@subscriber.time_zone).strftime('%b %d') %></span>
  <span class="activity_gravatar"><%= image_tag gravatar_url(activity.user), class: "img-circle", width: 50, height: 50 %></span>
  <strong class="label label-info">Update</strong>&nbsp;<%= user.display_name %> modified a time off request for <%= event_start %>.
  <hr/>
  </li>
<% end %>



